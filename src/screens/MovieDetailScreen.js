import React from 'react'
import { StyleSheet, Text, View } from 'react-native'

const MovieDetailScreen = () => {
    return (
        <View>
            <Text style={{ fontSize: 48 }}>MovieDetailScreen</Text>
        </View>
    )
}

export default MovieDetailScreen

const styles = StyleSheet.create({})
